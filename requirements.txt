# Pinned to the versions actually installed and used in .venv on this
# machine (CPU-only torch build; no CUDA/GPU packages required).
torch==2.14.0+cpu
deepspeed==0.19.7
pyspark==4.2.0
py4j==0.10.9.9
numpy==2.5.3
psutil==7.2.2
hjson==3.1.0
ninja==1.13.2
einops==0.8.2
pydantic==2.13.5
